# Copy to .env. Every variable is optional except the two passwords; the values shown for
# the optional ones are the defaults. All variables are described in docs/operations.md.

# ── ArangoDB ──────────────────────────────────────────────────────────────────
ARANGO_PASSWORD=changeme
ARANGO_ROOT_PASSWORD=changeme      # read by docker-compose.yml; keep equal to ARANGO_PASSWORD
# ARANGO_URL=http://localhost:8529
# ARANGO_DB_NAME=lawgraph          # created on first use when it does not exist
# ARANGO_USER=root
# LAWGRAPH_BACKUP_DIR=./backups          # where scripts/backup.sh writes its dumps (mounted by compose)
# LAWGRAPH_DB_SIZE_ALERT_GIB=70          # `lawgraph check` fails from this size on

# ── External sources ──────────────────────────────────────────────────────────
# TK_API_BASE=https://gegevensmagazijn.tweedekamer.nl/OData/v4/2.0/
# RECHTSPRAAK_BASE=https://data.rechtspraak.nl/
# EURLEX_BASE=https://eur-lex.europa.eu/
# EURLEX_SPARQL_ENDPOINT=https://publications.europa.eu/webapi/rdf/sparql
# BWB_BASE=https://wetten.overheid.nl/
# BWB_SRU_ENDPOINT=https://zoekservice.overheid.nl/sru/Search
# STAATSBLAD_SRU_ENDPOINT=https://repository.overheid.nl/sru
# STAATSBLAD_REPO_BASE=https://repository.overheid.nl
# STAATSCOURANT_SRU_ENDPOINT=https://repository.overheid.nl/sru
# STAATSCOURANT_REPO_BASE=https://repository.overheid.nl
# KAMERSTUK_REPO_BASE=https://repository.overheid.nl
# EERSTEKAMER_SRU=https://repository.overheid.nl/sru
# ECHR_HUDOC_BASE=https://hudoc.echr.coe.int
# VERDRAGENBANK_SRU=https://repository.overheid.nl/sru

# ── Pipelines ─────────────────────────────────────────────────────────────────
# BWB_IDS=BWBR0001854,BWBR0001903          # regulations for incremental `retrieve bwb`
# EURLEX_MAX_ARTICLE_NUMBER=200
# LAWGRAPH_CONFIDENCE_SCOPE_LIMITATION=0.8 # LAWGRAPH_CONFIDENCE_<PATTERN>
# LAWGRAPH_RETRIEVE_SKIP_ECHR=true         # LAWGRAPH_<PHASE>_SKIP_<SOURCE>

# ── API ───────────────────────────────────────────────────────────────────────
# LAWGRAPH_API_HOST=127.0.0.1
# LAWGRAPH_API_PORT=8000
# LAWGRAPH_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:5174,http://127.0.0.1:5174
# LAWGRAPH_RATE_LIMIT_CALLS=200
# LAWGRAPH_RATE_LIMIT_PERIOD=60
# LAWGRAPH_TRUSTED_PROXIES=
# LAWGRAPH_CACHE_TTL=60
# LAWGRAPH_CACHE_MAXSIZE=512
# LAWGRAPH_CURATION_API_KEY=               # unset: POST /api/relationships/tag answers 503
# LAWGRAPH_WRITE_API_KEY=                  # unset: watches and votes answer 503 (X-Write-Key)

# ── Logging ───────────────────────────────────────────────────────────────────
# LAWGRAPH_LOG_LEVEL=INFO
# LAWGRAPH_LOG_FORMAT=json
# LAWGRAPH_LOG_FILE=lawgraph.log
# NO_COLOR=1
